/*
    Helper test class for define address, init ballance and end ballance
*/
module.exports = class User {
    constructor(address, initBallance) {
        this.address = address
        this.initBallance = initBallance
    }
}